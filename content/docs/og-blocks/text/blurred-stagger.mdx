---
title: Staggered Blur
description: A staggered blur effect for text elements.
---

import { PreviewContainer } from "@/components/PreviewContainer";

import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

import { BlurredStagger } from "@/components/og-blocks/text/BlurredStagger";

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <PreviewContainer>
      <BlurredStagger text="HextaUI is awesome ❤️" />
    </PreviewContainer>
  </Tab>
  <Tab value="Code">
    ```tsx
    <BlurredStagger text="HextaUI is awesome ❤️" />
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
<Tab value="Manual">

Copy and paste the following code into your project.

```tsx title="components/ui/BlurredStagger.tsx"
"use client";

import { motion } from "motion/react";

export const BlurredStagger = ({
  text = "we love hextaui.com ❤️",
}: {
  text: string;
}) => {
  const headingText = text;

  const container = {
    hidden: { opacity: 0 },
    show: {
      opacity: 1,
      transition: {
        staggerChildren: 0.015,
      },
    },
  };

  const letterAnimation = {
    hidden: {
      opacity: 0,
      filter: "blur(10px)",
    },
    show: {
      opacity: 1,
      filter: "blur(0px)",
    },
  };

  return (
    <>
      <div>
        <motion.h1
          variants={container}
          initial="hidden"
          animate="show"
          className="text-base"
        >
          {headingText.split("").map((char, index) => (
            <motion.span
              key={index}
              variants={letterAnimation}
              transition={{ duration: 0.3 }}
            >
              {char === " " ? "\u00A0" : char}
            </motion.span>
          ))}
        </motion.h1>
      </div>
    </>
  );
};
```

</Tab>
<Tab value="CLI">

```package-install
npx shadcn@latest add "https://21st.dev/r/hextaui/blurred-stagger-text"
```

</Tab>
</Tabs>

## Usage

```tsx
import { BlurredStagger } from "@/components/ui/BlurredStagger";
```

```tsx
<BlurredStagger text="HextaUI is awesome ❤️" />
```

## Props

<TypeTable
  type={{
    text: {
      description: "The text to display with the staggered blur effect",
      type: "string",
      default: "we love hextaui.com ❤️",
    },
  }}
/>
