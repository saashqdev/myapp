---
title: Animated Underline
description: A component that adds an animated underline effect to text.
---

import { PreviewContainer } from "@/components/PreviewContainer";

import { TypeTable } from "fumadocs-ui/components/type-table";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

import { AnimatedUnderline } from "@/components/og-blocks/text/AnimatedUnderline";

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <PreviewContainer>
      <AnimatedUnderline text="Hover me" />
    </PreviewContainer>
  </Tab>
  <Tab value="Code">
    ```tsx
    <AnimatedUnderline text="Hover me" />
    ```
  </Tab>
</Tabs>

## Installation

<Tabs items={["Manual", "CLI"]} defaultIndex={1}>
<Tab value="Manual">

Copy and paste the following code into your project.

```tsx title="components/ui/AnimatedUnderline.tsx"
"use client";

export function AnimatedUnderline({ text = "Hover me" }: { text: string }) {
  return (
    <div className="relative after:absolute after:bg-foreground after:bottom-0 after:left-0 after:h-px after:w-full after:origin-bottom-right after:scale-x-0 hover:after:origin-bottom-left hover:after:scale-x-100 after:transition-transform after:ease-in-out after:duration-300">
      <span className="text-foreground">{text}</span>
    </div>
  );
}
```

</Tab>
<Tab value="CLI">

```package-install
npx shadcn@latest add "https://21st.dev/r/hextaui/animated-underline"
```

</Tab>
</Tabs>

## Usage

```tsx
import { AnimatedUnderline } from "@/components/ui/AnimatedUnderline";
```

```tsx
<AnimatedUnderline text="Hover me" />
```

## Props

<TypeTable
  type={{
    text: {
      type: "string",
      description: "The text to display with the animated underline effect.",
      default: "Hover me",
    },
  }}
/>
